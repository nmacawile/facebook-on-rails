<% content_for :title, "#{@user.full_name}" %>
<%= render "banner", active: :posts %>

<div class="container">
  <div class="row">
    
    <div class="col-md-3">
      <h3><%= @user.full_name %></h3>
      <h4>@<%= @user.username %></h4>
    </div>
    
    <%= render "users/posts/posts" %>
  </div>
</div>