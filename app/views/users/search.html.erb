<% content_for :title, "Search Results for #{params[:q]}" %>
<div class="container">
  <div class="row">
    <div class="col-md-3"></div>
    <%= render "shared/user_cards", title: "Search Results for: #{params[:q]}",
                                    message_if_empty: "No results." %>
  </div>
</div>